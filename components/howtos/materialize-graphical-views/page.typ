= materialize graphical views

== Run

```bash
nix build .#checks.x86_64-linux.materialize-graphical-views
cd components/howtos/materialize-graphical-views && ./run
```

== Input

Example files in this directory (see `*.cbs.txt`, `*.sml.txt`, `*.gel`).

== Output

Successful CBShell session without server errors.
