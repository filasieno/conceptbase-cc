= realize cascading untell via ecarules

== Run

```bash
nix build .#checks.x86_64-linux.realize-cascading-untell-via-ecarules
cd components/howtos/realize-cascading-untell-via-ecarules && ./run
```

== Input

Example files in this directory (see `*.cbs.txt`, `*.sml.txt`, `*.gel`).

== Output

Successful CBShell session without server errors.
