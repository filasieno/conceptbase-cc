= define recursive queries on cyclic structures

== Run

```bash
nix build .#checks.x86_64-linux.define-recursive-queries-on-cyclic-structures
cd components/howtos/define-recursive-queries-on-cyclic-structures && ./run
```

== Input

Example files in this directory (see `*.cbs.txt`, `*.sml.txt`, `*.gel`).

== Output

Successful CBShell session without server errors.
