= send an email via an active rule

== Run

```bash
nix build .#checks.x86_64-linux.send-an-email-via-an-active-rule
cd components/howtos/send-an-email-via-an-active-rule && ./run
```

== Input

Example files in this directory (see `*.cbs.txt`, `*.sml.txt`, `*.gel`).

== Output

Successful CBShell session without server errors.
