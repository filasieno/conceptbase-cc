= save telos models

== Run

```bash
nix build .#checks.x86_64-linux.save-telos-models
cd components/howtos/save-telos-models && ./run
```

== Input

Example files in this directory (see `*.cbs.txt`, `*.sml.txt`, `*.gel`).

== Output

Successful CBShell session without server errors.
