= specify resizable shapes

== Run

```bash
nix build .#checks.x86_64-linux.specify-resizable-shapes
cd components/howtos/specify-resizable-shapes && ./run
```

== Input

Example files in this directory (see `*.cbs.txt`, `*.sml.txt`, `*.gel`).

== Output

Successful CBShell session without server errors.
