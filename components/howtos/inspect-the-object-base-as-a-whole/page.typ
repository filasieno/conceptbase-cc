= inspect the object base as a whole

== Run

```bash
nix build .#checks.x86_64-linux.inspect-the-object-base-as-a-whole
cd components/howtos/inspect-the-object-base-as-a-whole && ./run
```

== Input

Example files in this directory (see `*.cbs.txt`, `*.sml.txt`, `*.gel`).

== Output

Successful CBShell session without server errors.
