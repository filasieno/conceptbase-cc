= deal with ocl style invariants

== Run

```bash
nix build .#checks.x86_64-linux.deal-with-ocl-style-invariants
cd components/howtos/deal-with-ocl-style-invariants && ./run
```

== Input

Example files in this directory (see `*.cbs.txt`, `*.sml.txt`, `*.gel`).

== Output

Successful CBShell session without server errors.
