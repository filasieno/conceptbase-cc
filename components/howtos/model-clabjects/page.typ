= model clabjects

== Run

```bash
nix build .#checks.x86_64-linux.model-clabjects
cd components/howtos/model-clabjects && ./run
```

== Input

Example files in this directory (see `*.cbs.txt`, `*.sml.txt`, `*.gel`).

== Output

Successful CBShell session without server errors.
