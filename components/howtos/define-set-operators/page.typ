= define set operators

== Run

```bash
nix build .#checks.x86_64-linux.define-set-operators
cd components/howtos/define-set-operators && ./run
```

== Input

Example files in this directory (see `*.cbs.txt`, `*.sml.txt`, `*.gel`).

== Output

Successful CBShell session without server errors.
