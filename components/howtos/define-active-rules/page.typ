= define active rules

== Run

```bash
nix build .#checks.x86_64-linux.define-active-rules
cd components/howtos/define-active-rules && ./run
```

== Input

Example files in this directory (see `*.cbs.txt`, `*.sml.txt`, `*.gel`).

== Output

Successful CBShell session without server errors.
