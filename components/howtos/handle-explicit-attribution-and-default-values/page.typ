= handle explicit attribution and default values

== Run

```bash
nix build .#checks.x86_64-linux.handle-explicit-attribution-and-default-values
cd components/howtos/handle-explicit-attribution-and-default-values && ./run
```

== Input

Example files in this directory (see `*.cbs.txt`, `*.sml.txt`, `*.gel`).

== Output

Successful CBShell session without server errors.
