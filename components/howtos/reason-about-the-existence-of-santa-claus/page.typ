= reason about the existence of santa claus

== Run

```bash
nix build .#checks.x86_64-linux.reason-about-the-existence-of-santa-claus
cd components/howtos/reason-about-the-existence-of-santa-claus && ./run
```

== Input

Example files in this directory (see `*.cbs.txt`, `*.sml.txt`, `*.gel`).

== Output

Successful CBShell session without server errors.
