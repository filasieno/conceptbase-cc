= model the entity relationship model

== Run

```bash
nix build .#checks.x86_64-linux.model-the-entity-relationship-model
cd components/howtos/model-the-entity-relationship-model && ./run
```

== Input

Example files in this directory (see `*.cbs.txt`, `*.sml.txt`, `*.gel`).

== Output

Successful CBShell session without server errors.
