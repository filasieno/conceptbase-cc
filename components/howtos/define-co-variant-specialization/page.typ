= define co variant specialization

== Run

```bash
nix build .#checks.x86_64-linux.define-co-variant-specialization
cd components/howtos/define-co-variant-specialization && ./run
```

== Input

Example files in this directory (see `*.cbs.txt`, `*.sml.txt`, `*.gel`).

== Output

Successful CBShell session without server errors.
