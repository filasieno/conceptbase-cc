= define formulas at the meta class level

== Run

```bash
nix build .#checks.x86_64-linux.define-formulas-at-the-meta-class-level
cd components/howtos/define-formulas-at-the-meta-class-level && ./run
```

== Input

Example files in this directory (see `*.cbs.txt`, `*.sml.txt`, `*.gel`).

== Output

Successful CBShell session without server errors.
