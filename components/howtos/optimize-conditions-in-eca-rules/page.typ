= optimize conditions in eca rules

== Run

```bash
nix build .#checks.x86_64-linux.optimize-conditions-in-eca-rules
cd components/howtos/optimize-conditions-in-eca-rules && ./run
```

== Input

Example files in this directory (see `*.cbs.txt`, `*.sml.txt`, `*.gel`).

== Output

Successful CBShell session without server errors.
