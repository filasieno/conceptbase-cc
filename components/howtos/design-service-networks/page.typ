= design service networks

== Run

```bash
nix build .#checks.x86_64-linux.design-service-networks
cd components/howtos/design-service-networks && ./run
```

== Input

Example files in this directory (see `*.cbs.txt`, `*.sml.txt`, `*.gel`).

== Output

Successful CBShell session without server errors.
