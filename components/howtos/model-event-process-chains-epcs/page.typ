= model event process chains epcs

== Run

```bash
nix build .#checks.x86_64-linux.model-event-process-chains-epcs
cd components/howtos/model-event-process-chains-epcs && ./run
```

== Input

Example files in this directory (see `*.cbs.txt`, `*.sml.txt`, `*.gel`).

== Output

Successful CBShell session without server errors.
