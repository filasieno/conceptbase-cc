= define basic properties of relations

== Run

```bash
nix build .#checks.x86_64-linux.define-basic-properties-of-relations
cd components/howtos/define-basic-properties-of-relations && ./run
```

== Input

Example files in this directory (see `*.cbs.txt`, `*.sml.txt`, `*.gel`).

== Output

Successful CBShell session without server errors.
