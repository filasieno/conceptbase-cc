= generate business models

== Run

```bash
nix build .#checks.x86_64-linux.generate-business-models
cd components/howtos/generate-business-models && ./run
```

== Input

Example files in this directory (see `*.cbs.txt`, `*.sml.txt`, `*.gel`).

== Output

Successful CBShell session without server errors.
