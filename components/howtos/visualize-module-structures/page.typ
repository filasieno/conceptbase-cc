= visualize module structures

== Run

```bash
nix build .#checks.x86_64-linux.visualize-module-structures
cd components/howtos/visualize-module-structures && ./run
```

== Input

Example files in this directory (see `*.cbs.txt`, `*.sml.txt`, `*.gel`).

== Output

Successful CBShell session without server errors.
