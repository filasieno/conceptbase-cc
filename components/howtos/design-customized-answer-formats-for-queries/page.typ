= design customized answer formats for queries

== Run

```bash
nix build .#checks.x86_64-linux.design-customized-answer-formats-for-queries
cd components/howtos/design-customized-answer-formats-for-queries && ./run
```

== Input

Example files in this directory (see `*.cbs.txt`, `*.sml.txt`, `*.gel`).

== Output

Successful CBShell session without server errors.
