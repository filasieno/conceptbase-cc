= formulate query calls with other queries as parameters

== Run

```bash
nix build .#checks.x86_64-linux.formulate-query-calls-with-other-queries-as-parameters
cd components/howtos/formulate-query-calls-with-other-queries-as-parameters && ./run
```

== Input

Example files in this directory (see `*.cbs.txt`, `*.sml.txt`, `*.gel`).

== Output

Successful CBShell session without server errors.
