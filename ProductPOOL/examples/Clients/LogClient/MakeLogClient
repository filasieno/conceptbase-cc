
override CB_HOME=/home/cbase/CB_NewStruct/CB_Product

override LIBDIR=$(CB_HOME)/sun4/lib/
override INCDIR=$(CB_HOME)/include

vpath %.c src

LogClient: LogClient.o
	gcc -Wall -O2 -o $@ -L$(LIBDIR) $< -lCB -lnsl -lsocket


LogClient.o: LogClient.c
	gcc -Wall -O2 -I$(INCDIR) -c $< -o $@


clean:
	rm LogClient LogClient.o
