# One-time archive tools

Scripts used during the greenfield migration. **Not part of the build** — kept for history only.

| Script | Purpose |
|--------|---------|
| `migrate-tex-to-typst.py` | Converted `ProductPOOL/doc/{UserManual,ProgManual,Tutorial}` LaTeX to `components/doc/` Typst via pandoc. Run once; superseded by direct Typst authoring. |
