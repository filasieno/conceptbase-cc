# Test queries whose arguments start with _, e.g. listModule[_1/module]
# Call like
#    cbshell ticket360.cbs
#
# Problem lies with buildTokens used for prolog_parser in TellAndAsk.pro




startServer -u nonpersistent -t low 


tell "
_1 in Module end
"
cd _1

tell "blabla end"

cd oHome

ask get_object[_1/objname]
# still fails due to some bug in query processor

ask listModule[_1/module]
# should work

ask listModule[abc/module]
# should trigger an error message since abc does not exist

ask listModule[System-oHome-_1/module]
# should work

ask listModule[System-xHome-_1/module]
# old CB version:lists the content of System because the path refers to non-existing module xHome
# 2016-06-01: return {* no *} since the path does not exist






